{
  "version": 2,
  "builds": [
    { "src": "api/**/*.js", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/$1.js" },
    { "src": "/list/(.*)", "dest": "/public/list/$1" },
    { "src": "/(.*)", "dest": "/public/$1" }
  ]
}
